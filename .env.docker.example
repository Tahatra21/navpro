# Salin ke .env.docker untuk `docker compose` (jangan commit .env.docker)
# Local development only — generate values: openssl rand -base64 24

POSTGRES_PASSWORD=
JWT_SECRET=
SEED_DEMO_PASSWORD=
MINIO_ROOT_PASSWORD=
MINIO_ROOT_USER=navpro
